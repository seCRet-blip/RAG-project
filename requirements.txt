# Backend (pin starlette — fasthtml must not pull starlette 1.x or FastAPI breaks)
fastapi>=0.115.0,<0.116.0
starlette>=0.40.0,<0.50.0
uvicorn[standard]>=0.32.0,<0.35.0
pydantic-settings>=2.6.0

# Frontend
python-fasthtml>=0.12.0,<0.13.0

# Retriever & vector store
qdrant-client>=1.12.0,<1.14.0
fastembed>=0.5.0

# LLM client (vLLM OpenAI-compatible API)
openai>=1.55.0
httpx>=0.28.0

# Document processing & crawling (all free / local)
pypdf>=5.1.0
python-multipart>=0.0.17
beautifulsoup4>=4.12.0
lxml>=5.3.0

# Dev
pytest>=8.3.0
pytest-asyncio>=0.24.0
